(ns credit-card-with-datomic.core-test
  (:require [clojure.test :refer :all]
            [credit-card-with-datomic.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
